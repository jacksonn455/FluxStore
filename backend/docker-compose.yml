version: '3.8'
services:
  redis:
    image: redis:7.2-alpine
    container_name: fluxstore-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - fluxstore-network

  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: fluxstore-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: password
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - fluxstore-network

volumes:
  redis_data:
  rabbitmq_data:

networks:
  fluxstore-network:
    driver: bridge